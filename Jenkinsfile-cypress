pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
                {
                    script {
                        sh 'npm install'
                    }
                }
            }
        }
        stage('Run Cypress Tests') {
            steps {
                script {
                    sh 'npx cypress run'
                }
            }
        }
    }
}
